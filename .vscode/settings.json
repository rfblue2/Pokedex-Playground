// Place your settings in this file to overwrite default and user settings.
{
  "files.exclude": {
    "**/node_modules/": true,
    "**/typings/": true,
    "**/*.js": {"when": "$(basename).ts"},
    "**/*.js.map": true
  }
}